{
  "name": "DWA Starter Vue",
  "tests": {
    "pre": [
      "npm install -g pnpm", 
      "pnpm install", 
      "npx playwright install --with-deps"
    ],
    "command": "pnpm run build && pnpm run test:unit && pnpm run test:e2e"
  }
}
